{
  "common": {
    "language": {
      "supported": ["en", "id"],
      "fallback": "en",
      "fixed": false
    },
    "detector": {
      "method": ["querystring", "path", "session", "cookie", "header"],
      "fieldName": "lang",
      "cache": ["cookie"]
    }
  }
}